plugins {
    id 'java'
    id 'idea'
}

allprojects {
    repositories {
        jcenter()
    }
    compileJava {
        sourceCompatibility = '1.8'
        targetCompatibility = '1.8'
    }
    group 'dev.squaremile'
}

subprojects {
    java {
        modularity.inferModulePath = true
    }
}